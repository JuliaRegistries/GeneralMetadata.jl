augment_platform_block = """using Base.BinaryPlatforms\n\ntry\n    using CUDA_Runtime_jll\ncatch\n    # during initial package installation, CUDA_Runtime_jll may not be available.\n    # in that case, we just won't select an artifact.\nend\n\n# can't use Preferences for the same reason\nconst CUDA_Runtime_jll_uuid = Base.UUID(\"76a88914-d11a-5bdc-97e0-2f5a05c973a2\")\nconst preferences = Base.get_preferences(CUDA_Runtime_jll_uuid)\nBase.record_compiletime_preference(CUDA_Runtime_jll_uuid, \"version\")\nBase.record_compiletime_preference(CUDA_Runtime_jll_uuid, \"local\")\n# the `local` preference is a boolean, but CUDA.jl's `set_runtime_version!` writes it\n# as a string, and hand-written LocalPreferences.toml files use either spelling.\nconst local_toolkit = let pref = get(preferences, \"local\", false)\n    pref isa Bool ? pref : something(tryparse(Bool, pref), false)\nend\n\nfunction cuda_comparison_strategy(_a::String, _b::String, a_requested::Bool, b_requested::Bool)\n    # if we're using a local toolkit, we can't use artifacts\n    if local_toolkit\n        return false\n    end\n\n    # if either isn't a version number (e.g. \"none\"), perform a simple equality check\n    a = tryparse(VersionNumber, _a)\n    b = tryparse(VersionNumber, _b)\n    if a === nothing || b === nothing\n        return _a == _b\n    end\n\n    # if both b and a requested, then we fall back to equality\n    if a_requested && b_requested\n        return Base.thisminor(a) == Base.thisminor(b)\n    end\n\n    # otherwise, do the comparison between the the single version cap and the single version:\n    function is_compatible(artifact::VersionNumber, host::VersionNumber)\n        if host >= v\"11.0\"\n            # enhanced compatibility, semver-style\n            artifact.major == host.major &&\n            Base.thisminor(artifact) <= Base.thisminor(host)\n        else\n            Base.thisminor(artifact) == Base.thisminor(host)\n        end\n    end\n    if a_requested\n        is_compatible(b, a)\n    else\n        is_compatible(a, b)\n    end\nend\n\nfunction augment_platform!(platform::Platform)\n    if !@isdefined(CUDA_Runtime_jll)\n        # don't set to nothing or Pkg will download any artifact\n        platform[\"cuda\"] = \"none\"\n    end\n\n    if !haskey(platform, \"cuda\")\n        CUDA_Runtime_jll.augment_platform!(platform)\n    end\n    BinaryPlatforms.set_compare_strategy!(platform, \"cuda\", cuda_comparison_strategy)\n\n    return platform\nend"""
init_block = ""
julia_compat = "1.6"
lazy_artifacts = true
name = "cuQuantum"
platforms = ["x86_64-linux-gnu-cuda_platform+sbsa-cuda+12.0", "aarch64-linux-gnu-cuda_platform+sbsa-cuda+12.0", "x86_64-linux-gnu-cuda+13.0", "aarch64-linux-gnu-cuda+13.0"]
script = """mkdir -p ${libdir} ${prefix}/include\n\ncd ${WORKSPACE}/srcdir/cuquantum-*\ninstall_license LICENSE\n\nmv lib/*.so* ${libdir}\nmv include/* ${prefix}/include\n"""
toplevel_block = ""
version = "v26.9.0"

[[dependencies]]
compat = ""
name = "CUDA_Runtime_jll"
platforms = ["any"]
top_level = false
type = "runtimedependency"
version-major = 0
version-minor = 0
version-patch = 0
[[dependencies]]
compat = "2.8.1"
name = "CUTENSOR_jll"
platforms = ["any"]
top_level = false
type = "runtimedependency"
version-major = 0
version-minor = 0
version-patch = 0

[[products]]
dir_paths = []
dlopen_flags = []
dont_dlopen = false
libnames = ["libcustatevec"]
type = "lib"
variable_name = "libcustatevec"
[[products]]
dir_paths = []
dlopen_flags = []
dont_dlopen = false
libnames = ["libcutensornet"]
type = "lib"
variable_name = "libcutensornet"
[[products]]
dir_paths = []
dlopen_flags = []
dont_dlopen = false
libnames = ["libcudensitymat"]
type = "lib"
variable_name = "libcudensitymat"
[[products]]
dir_paths = []
dlopen_flags = []
dont_dlopen = false
libnames = ["libcupauliprop"]
type = "lib"
variable_name = "libcupauliprop"
[[products]]
dir_paths = []
dlopen_flags = []
dont_dlopen = false
libnames = ["libcustabilizer"]
type = "lib"
variable_name = "libcustabilizer"

[[sources]]
hash = "b908cc21b032a83cf3c157a00e5fb230efa54035920d20a73c16568e6ee17ad7"
type = "archive"
unpack_target = ""
url = "https://developer.download.nvidia.com/compute/cuquantum/redist/cuquantum/linux-x86_64/cuquantum-linux-x86_64-26.09.0.8_cuda12-archive.tar.xz"
[[sources]]
hash = "23089657fd50470cdcfbccb48acc36e501ec3b927fa58279b8729a85e99078cb"
type = "archive"
unpack_target = ""
url = "https://developer.download.nvidia.com/compute/cuquantum/redist/cuquantum/linux-sbsa/cuquantum-linux-sbsa-26.09.0.8_cuda12-archive.tar.xz"
[[sources]]
hash = "92ecbba2b5783db20a966aa67e24206501eef3907ec09dd51cfb4753aea65731"
type = "archive"
unpack_target = ""
url = "https://developer.download.nvidia.com/compute/cuquantum/redist/cuquantum/linux-x86_64/cuquantum-linux-x86_64-26.09.0.8_cuda13-archive.tar.xz"
[[sources]]
hash = "5698f5a97572e35653d55d87fc15428ea4a10911e711b82e0d5162efc152db67"
type = "archive"
unpack_target = ""
url = "https://developer.download.nvidia.com/compute/cuquantum/redist/cuquantum/linux-sbsa/cuquantum-linux-sbsa-26.09.0.8_cuda13-archive.tar.xz"
